{
  "name": "crazy-leather-things",
  "private": true,
  "scripts": {
    "start": "npm-run-all --parallel server client",
    "server": "npm start --prefix server",
    "client": "npm run dev --prefix client",
    "build": "npm install --prefix client && npm run build --prefix client"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}